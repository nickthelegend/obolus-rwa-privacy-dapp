# Architecture

> Auto-generated by /map on 2026-03-28

## Overview

Obolus Landing Page is a single-page interactive application built with Next.js and Tailwind CSS 4. It highlights the Obolus Confidential Equity Vault project on BNB Chain, featuring animated sections with Framer Motion and a "Neon Lime" design theme.

```markdown
┌─────────────────────────────────────────┐
│              [entry-point]              │
│               app/page.tsx              │
├─────────────────────────────────────────┤
│         [design-tokens]                 │
│         app/globals.css                 │
├─────────────────────────────────────────┤
│            [components]                 │
│       inline-components (Logo, etc)     │
└─────────────────────────────────────────┘
```

## Components

### Home Page
- **Purpose:** Hero section, partner features, and product showcase.
- **Location:** `app/page.tsx`
- **Dependencies:** `framer-motion`, `next/link`, `next/image`

### Global CSS
- **Purpose:** Defines Tailwind 4 `@theme` with specific project colors and fonts.
- **Location:** `app/globals.css`
- **Colors:** Neon Lime (`#ccff00`), Black (`#000000`), Light gray (`#f7f8f6`).

## Data Flow

1. User visits `/`
2. Next.js server handles the route and serves the landing page.
3. Client-side animations (Framer Motion) trigger on view.
4. User clicks "Launch App" or "Get Credit" to navigate to external links (future integration).

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Chainlink CCIP | SDK/Message | Cross-chain collateral tracking |
| Reclaim Protocol | Zero-Knowledge | Sybil resistance and trust |
| fhEVM | Smart Contract | Privacy-preserving equity vaults (BNB Chain) |

## Conventions

**Naming:** PascalCase for React components, kebab-case for assets.
**Structure:** App router with global styles and inline components for the prototype phase.
**CSS:** Tailwind 4 with CSS variable theme in `globals.css`.
